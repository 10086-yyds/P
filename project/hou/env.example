# 环境配置
NODE_ENV=development

# 服务器配置
PORT=3000

# MongoDB 配置
# 本地 MongoDB
MONGODB_URI=mongodb://localhost:27017/hou_dev

# 或者使用 MongoDB Atlas (需要替换为实际的连接字符串)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/hou_dev

# 数据库配置 (备用)
DB_HOST=localhost
DB_PORT=27017
DB_NAME=hou_dev
DB_USERNAME=your_username
DB_PASSWORD=your_password

# 安全配置
SESSION_SECRET=your-super-secret-session-key-change-this-in-production
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001 